# Grok (`tools/grok/`)

| Path | Live path | Notes |
|------|-----------|--------|
| `config.toml` | `~/.grok/config.toml` | Adopted on first setup if present (auth stays in `~/.grok/auth.json`) |
| `hooks/` | `~/.grok/hooks` | Whole-dir symlink |

Skills and commands: harness root. Rules: `AGENTS.md` → `~/.grok/AGENTS.md`.
