# OpenCode (`tools/opencode/`)

| Path | Live path | Notes |
|------|-----------|--------|
| `opencode.json` | `~/.config/opencode/opencode.json` | Adopted on first setup if present |
| `plugins/` | `~/.config/opencode/plugins` | Whole-dir symlink |

Skills and commands live at the harness root (`skills/`, `commands/`), not here.
